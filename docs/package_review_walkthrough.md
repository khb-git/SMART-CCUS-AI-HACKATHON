# Package Review Demo Walkthrough

This walkthrough shows how to run the SMART CCUS package review workflow and interpret the package-level results.

## 1. Start the backend

```powershell
python -m uvicorn api.main:app --reload